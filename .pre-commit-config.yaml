repos:
  - repo: local
    hooks:
      - id: check-secrets
        name: check-secrets (no leaked AWS account IDs)
        entry: python3 scripts/check_secrets.py
        language: system
        pass_filenames: false
        always_run: true
